const baseAuthPath = 'auth'
const home = '/'
const auth = {
    login: baseAuthPath + '/login',
    signUp: baseAuthPath + '/register'
}

const routesName = {
    home, auth
}

export default routesName;